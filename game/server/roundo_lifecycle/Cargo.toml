[package]
name = "roundo_lifecycle"
version.workspace = true
edition.workspace = true

[dependencies]
bevy.workspace = true

[features]
dev = [
    "bevy/debug",
    "bevy/file_watcher",
]
release = [

]
